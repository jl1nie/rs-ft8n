[package]
name = "ft8-core"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[features]
default = ["parallel"]
parallel = ["rayon"]

[dependencies]
rustfft = "6"
num-complex = "0.4"
crc = "3"
rayon = { version = "1", optional = true }
